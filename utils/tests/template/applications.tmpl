template Applications(var products) {
  <div>
    ${#each products}
      <div>
        <span>${name}</span>
        <span>-</span>
        <span>${users}</span>
      </div>
    ${/each}
  </div>
}

