name: package_under_test
version: 0.0.1
description: Package under test for macros build tests.
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  macros: any

dev_dependencies:
  test: any

dependency_overrides:
  macros:
    path: ../../../pkg/macros
