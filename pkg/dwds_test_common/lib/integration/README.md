# DWDS Shared Integration Tests

This directory contains shared integration test scenarios for DWDS.

These tests are meant to be provided a build context by the test caller, such as `frontend_server` or `build_daemon`, allowing the same debugging and inspection workflows to be verified across different build clients.
