String console = "";

class A {
  String? get g => "g";
}

class C implements A {
  noSuchMethod(Invocation i) {}
}

class MA extends C with M {}

expect(expected, actual) {}

main() {}

mixin M on A {
  noSuchMethod(Invocation i) {}
  test() {}
}
