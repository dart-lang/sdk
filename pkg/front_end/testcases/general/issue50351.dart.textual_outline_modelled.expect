String? something() => 'something';

class C {
  var value = () {
    var value = something();
    if (value == null) {
      throw '';
    }
    var result = value;
    return result;
  }();
}

void main() {}
