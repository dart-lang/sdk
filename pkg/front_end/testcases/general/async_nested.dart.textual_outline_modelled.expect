import 'dart:async';

class Node {
  Node(this.name, [this.nested]) {}
  String toString() => '<$name:[${nested?.join(', ')}]>';
  final List<Node> nested;
  final String name;
  toSimpleString() {}
}

void main() async {}
