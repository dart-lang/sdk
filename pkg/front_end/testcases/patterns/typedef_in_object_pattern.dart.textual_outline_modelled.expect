class A {
  A(this.foo);
  int foo;
}

class C<X, Y> {
  C(this.x, this.y);
  X x;
  Y y;
}

expectEquals(x, y) {}

main() {}

test1(dynamic x) {}

test2(dynamic x) {}

typedef B = A;

typedef D<X> = C<X, X>;
