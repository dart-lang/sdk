expect(expected, actual) {}
inline class I {
  I(int x, [int? y]) : it = x + (y ?? 42);
  final int it;
  void m(String s, [int i = 1]) {}
}
inline class I2 {
  I2(int x, {int? y}) : it = x + (y ?? 87);
  final int it;
  void m(String s, {int i = 1}) {}
}
main() {}
