class A<X> {
  X field;
  A(this.field);
}

abstract class B<Y> implements A<Y> {
  get field;
  set field(value);
}

abstract class C implements A<int> {
  get field;
  set field(value);
}

main() {}
