class B implements A {
  final int x = 1;
}

abstract class A {
  int get x;
  factory A() = B;
}

extension type C._(A point) implements A {
  C() : point = A();
}

void main() {}

expectEquals(x, y) {}
