library test;

class B<T> {
  T x = throw '';
  T y = throw '';
}

abstract class C<T> implements B<num> {
  var x = throw '';
  get y;
  set y(value);
}

abstract class D<T> implements B<T> {
  var x = throw '';
  get y;
  set y(value);
}

main() {}
