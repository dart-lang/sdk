class A {
  const A({required this.b});
  final B b;
}

class B {
  const B({required this.c});
  factory B.redirect({required C c}) = B;
  final C c;
}

class C {
  C.f(num n);
}

final a = A(b: .redirect(c: .f(0)));
