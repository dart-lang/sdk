name: dart_app

publish_to: none

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  native_add:
    path: ../native_add
  native_subtract:
    path: ../native_subtract

dependency_overrides:
  native_assets_cli:
    path: ../../../../../third_party/pkg/native/pkgs/native_assets_cli/
