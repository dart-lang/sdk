name: status_file
# This package is not intended for consumption on pub.dev. DO NOT publish.
publish_to: none
environment:
  sdk: "^2.12.0"
dependencies:
  path: "^1.4.0"
  args: ^2.0.0

dev_dependencies:
  expect:
    path: ../expect
